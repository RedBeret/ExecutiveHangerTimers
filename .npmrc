# Fail fast when the active Node/npm is outside the range in package.json "engines".
# Without this, npm starts the build on an unsupported runtime and it dies later
# inside a transitive dependency with an unrelated-looking error.
engine-strict=true
